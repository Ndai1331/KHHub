@model IReadOnlyList<PublicContentCardViewModel>

<div class="kh-list" data-view-panel="list">
    @foreach (var item in Model)
    {
        <partial name="~/Pages/Shared/PublicContent/_CardItem.cshtml" model="item" />
    }
</div>
