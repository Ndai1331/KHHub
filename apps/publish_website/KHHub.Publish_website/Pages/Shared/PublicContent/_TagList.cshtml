@model IReadOnlyList<string>

@if (Model.Count > 0)
{
    <ul class="kh-tags" aria-label="Tags">
        @foreach (var tag in Model)
        {
            <li><a href="?Tags=@Uri.EscapeDataString(tag)">#@tag</a></li>
        }
    </ul>
}
